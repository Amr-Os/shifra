# أول برنامج بلغة عربية مبنية فوق RustPython.

عرف مربع(عدد):
    أعد عدد * عدد

نتيجة = مربع(7)

إن نتيجة == 49:
    print("hi there my friend")
    print("نجح البرنامج العربي:", نتيجة)
وإلا:
    print("حدث خطأ")
